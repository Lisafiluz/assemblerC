//
// Created by Iluz, Lisaf(AWF) on 24/02/2023.
//

#include "assemblyEncoder.h"

int saveGuidanceLine(char *line, short *dataArray) {
    // split the line into tokens
//    if (isDotData(firstWord)) {
//// save the data to the memory and update dc
//    } else if (isDotData(secondWord)) {
//// save the data to the memory and update dc
//    } else if (isDotString(firstWord)) {
//// save the data to the memory and update dc
//    } else {
//// save the data to the memory and update dc
//    }
}